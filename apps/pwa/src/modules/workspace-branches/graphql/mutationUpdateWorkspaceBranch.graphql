#import "../graphql/fragmentWorkspaceBranch.graphql"

mutation UpdateWorkspaceBranch($input: WorkspaceBranchInput!, $id: String!) {
  updateWorkspaceBranch(input: $input, id: $id) {
    ...WorkspaceBranch
  }
}
