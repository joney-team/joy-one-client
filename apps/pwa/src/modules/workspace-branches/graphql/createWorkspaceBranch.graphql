#import "../graphql/fragmentWorkspaceBranch.graphql"

mutation CreateWorkspaceBranch($input: WorkspaceBranchInput!) {
  workspaceBranch: createWorkspaceBranch(input: $input) {
    ...WorkspaceBranch
  }
}
