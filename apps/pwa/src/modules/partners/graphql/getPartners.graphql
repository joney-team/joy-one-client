#import "./fragmentPartner.graphql"

query GetPartners(
  $query: JSONObject
  $ids: [String!]
  $limit: Float
  $offset: Float
) {
  list: getPartners(query: $query, ids: $ids, limit: $limit, offset: $offset) {
    total
    results {
      ...Partner
    }
  }
}
