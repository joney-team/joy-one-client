#import "./fragmentLoan.graphql"

query Loans(
  $limit: Float
  $offset: Float
  $query: JSONObject
) {
  list: loans(
    limit: $limit
    offset: $offset
    query: $query
  ) {
    total
    results {
      ...Loan
    }
  }
}
