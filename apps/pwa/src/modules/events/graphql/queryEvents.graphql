#import "./fragmentEvent.graphql"

query Events(
  $limit: Float
  $offset: Float
  $ref: String
  $userId: String
  $type: EventType
) {
  events(
    limit: $limit
    offset: $offset
    ref: $ref
    userId: $userId
    type: $type
  ) {
    total
    results {
      ...Event
    }
  }
}
