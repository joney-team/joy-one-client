#import "./fragmentEvent.graphql"

query GetEvents(
  $query: EventsQuery
  $ids: [String!]
  $limit: Float
  $offset: Float
) {
  list: getEvents(query: $query, ids: $ids, limit: $limit, offset: $offset) {
    total
    results {
      ...Event
    }
  }
}
