query TaskStatuses($contextType: TaskStatusesContextType, $contextId: String) {
  taskStatuses(contextType: $contextType, contextId: $contextId) {
    isCustomized
    statuses {
      id
      name
      color
      order
      contextId
      contextType
    }
  }
}
