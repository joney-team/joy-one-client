query TaskMetrics($contextType: TaskContextType!, $contextId: String) {
  taskMetrics(contextType: $contextType, contextId: $contextId) {
    contextType
    contextId
    estimatedTime
    progress
    totalTasks
    inProgressTasks
    startDate
    dueDate
    overdueTasks
  }
}
