#import "../../workspace-members/graphql/fragmentWorkspaceMember.graphql"

fragment TaskTimeTracking on TaskTimeTracking {
  id
  workspaceId
  userId
  user {
    ...WorkspaceMember
  }
  note
  startAt
  endAt
}
