#import "../../workspace-members/graphql/fragmentWorkspaceMember.graphql"

fragment TaskTimeTrackingData on TaskTimeTracking {
  id
  workspaceId
  userId
  user {
    ...WorkspaceMemberData
  }
  note
  startAt
  endAt
}
