#import "./fragmentAttendanceRecord.graphql"

query AttendanceRecords($limit: Float, $offset: Float, $query: JSONObject) {
  attendanceRecords(limit: $limit, offset: $offset, query: $query) {
    total
    results {
      ...AttendanceRecord
    }
  }
}