#import "./fragmentWorkspaceRole.graphql"

mutation CreateWorkspaceRole($name: String!, $permissions: [String!]!, $description: String, $color: String) {
  createWorkspaceRole(name: $name, permissions: $permissions, description: $description, color: $color) {
    ...WorkspaceRole
  }
}