#import "./fragmentReceipt.graphql"

query Receipts($limit: Float, $offset: Float, $query: JSONObject) {
  list: receipts(limit: $limit, offset: $offset, query: $query) {
    total
    results {
      ...Receipt
    }
  }
}
