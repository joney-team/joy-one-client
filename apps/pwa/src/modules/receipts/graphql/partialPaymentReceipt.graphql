#import "../graphql/fragmentReceipt.graphql"

mutation PartialPaymentReceipt(
  $partialPaymentReceiptId: String!
  $input: PartialPaymentInput!
) {
  receipt: partialPaymentReceipt(id: $partialPaymentReceiptId, input: $input) {
    ...Receipt
  }
}
