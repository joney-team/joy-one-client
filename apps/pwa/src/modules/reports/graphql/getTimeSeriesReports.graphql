#import "./fragmentTimeSeriesReport.graphql"

query GetTimeSeriesReports($input: ExportTimeSeriesReportInput!) {
  list: getTimeSeriesReports(input: $input) {
    total
    results {
      ...TimeSeriesReport
    }
  }
}
