#import "./fragmentCustomerForm.graphql"

query CustomerForms($limit: Float, $offset: Float, $query: JSONObject) {
  list: customerForms(limit: $limit, offset: $offset, query: $query) {
    total
    results {
      ...CustomerFormData
    }
  }
}
