#import "./fragmentCustomerForm.graphql"

mutation UpdateCustomerForm($formId: String!, $input: CustomerFormInput!) {
  customerForm: updateCustomerForm(id: $formId, input: $input) {
    ...CustomerForm
  }
}