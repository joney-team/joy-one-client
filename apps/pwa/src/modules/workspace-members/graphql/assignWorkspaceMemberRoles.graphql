#import "./fragmentWorkspaceMember.graphql"

mutation AssignWorkspaceMemberRoles($memberId: String!, $roleIds: [String!]!) {
  member: assignWorkspaceMemberRoles(memberId: $memberId, roleIds: $roleIds) {
    ...WorkspaceMember
  }
}
