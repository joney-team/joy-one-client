#import "./fragmentWorkspaceMember.graphql"

query WorkspaceMembers($ignoreSelf: Boolean, $ids: [String!]) {
  workspaceMembers(ignoreSelf: $ignoreSelf, ids: $ids) {
    data {
      ...WorkspaceMemberData
    }
    count
  }
}