#import "./fragmentWorkspaceMember.graphql"

mutation UpdateWorkspaceMember(
  $input: UpdateWorkspaceMemberInput!
  $memberId: String!
) {
  updateWorkspaceMember(input: $input, memberId: $memberId) {
    ...WorkspaceMember
  }
}
