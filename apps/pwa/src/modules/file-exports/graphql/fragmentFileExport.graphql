#import "../../workspace-members/graphql/fragmentWorkspaceMember.graphql"

fragment FileExport on FileExport {
  _id
  contextType
  contextArgs
  fileName
  status
  error
  createdAt
  updatedAt
  userId
  createdByUser {
    ...WorkspaceMember
  }
}
