#import "./fragmentFileExport.graphql"

query GetFileExports($limit: Float, $offset: Float, $query: JSONObject) {
  list: getFileExports(limit: $limit, offset: $offset, query: $query) {
    total
    results {
      ...FileExport
    }
  }
}
