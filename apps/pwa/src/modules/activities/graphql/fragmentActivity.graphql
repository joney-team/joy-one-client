#import "../../workspace-members/graphql/fragmentWorkspaceMember.graphql"

fragment ActivityData on Activity {
  _id
  contextType
  contextId
  type
  content
  data
  parentId
  childCount
  isPinned
  createdAt
  reactionsCount {
    reactions {
      userIds
      type
      count
    }
  }
  createdByUser {
    ...WorkspaceMemberData
  }
}
