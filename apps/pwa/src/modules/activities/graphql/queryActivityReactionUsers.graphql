query ActivityReactionUsers(
  $ignoreSelf: Boolean
  $userIds: [String!]
  $limit: Float
  $offset: Float
) {
  users: workspaceMembers(
    ignoreSelf: $ignoreSelf
    userId: $userIds
    limit: $limit
    offset: $offset
  ) {
    data {
      _id
      name
    }
    count
  }
}
