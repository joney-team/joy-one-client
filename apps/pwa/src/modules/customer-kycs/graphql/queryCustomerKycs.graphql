#import "./fragmentCustomerKyc.graphql"

query CustomerKycs($limit: Float, $offset: Float, $query: JSONObject) {
  list: customerKycs(limit: $limit, offset: $offset, query: $query) {
    total
    results {
      ...CustomerKycData
    }
  }
}
