#import "./fragmentPromotion.graphql"

mutation UpdatePromotionStatus(
  $promotionId: String!
  $status: PromotionStatus!
) {
  promotion: updatePromotionStatus(promotionId: $promotionId, status: $status) {
    ...Promotion
  }
}
