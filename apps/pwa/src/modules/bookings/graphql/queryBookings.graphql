#import "./fragmentBooking.graphql"

query Bookings($limit: Float, $offset: Float, $query: JSONObject) {
  list: bookings(limit: $limit, offset: $offset, query: $query) {
    total
    results {
      ...BookingData
    }
  }
}