#import "./fragmentTag.graphql"

query Tags($type: String, $offset: Float, $limit: Float, $ids: [String!]) {
  tags(type: $type, offset: $offset, limit: $limit, ids: $ids) {
    total
    results {
      ...Tag
    }
  }
}
