#import "./fragmentTag.graphql"

query GetTags(
  $query: GetTagsQuery
  $ids: [String!]
  $limit: Float
  $offset: Float
) {
  list: getTags(query: $query, ids: $ids, limit: $limit, offset: $offset) {
    total
    results {
      ...Tag
    }
  }
}
