#import "./fragmentTag.graphql"

query GetTags($type: String, $offset: Float, $limit: Float, $ids: [String!]) {
  list: getTags(type: $type, offset: $offset, limit: $limit, ids: $ids) {
    total
    results {
      ...Tag
    }
  }
}
