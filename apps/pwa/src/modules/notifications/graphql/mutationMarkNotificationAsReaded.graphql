#import "./fragmentNotification.graphql"

mutation MarkNotificationAsReaded($markNotificationAsReadedId: String!) {
  markNotificationAsReaded(id: $markNotificationAsReadedId) {
    ...Notification
  }
}
