#import "./fragmentCustomer.graphql"

query Customers($ids: [String!], $limit: Float, $offset: Float) {
  list: customers(ids: $ids, limit: $limit, offset: $offset) {
    total
    results {
      ...CustomerData
    }
  }
}
