#import "./fragmentMessageBox.graphql"

query GetMessageBoxes(
  $query: JSONObject
  $ids: [String!]
  $limit: Float
  $offset: Float
) {
  list: getMessageBoxes(query: $query, ids: $ids, limit: $limit, offset: $offset) {
    total
    results {
      ...MessageBox
    }
  }
}
