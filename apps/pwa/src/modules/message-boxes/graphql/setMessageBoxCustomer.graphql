#import "./fragmentMessageBox.graphql"

mutation SetMessageBoxCustomer($boxId: String!, $customerId: String) {
  setMessageBoxCustomer(boxId: $boxId, customerId: $customerId) {
    ...MessageBox
  }
}