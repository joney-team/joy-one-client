mutation PluginExternalStorageSignUploadUrl(
  $fileName: String!
  $refs: [String!]
  $id: String
) {
  pluginExternalStorageSignUploadUrl(
    fileName: $fileName
    refs: $refs
    id: $id
  ) {
    dna
    signedUrl
  }
}
